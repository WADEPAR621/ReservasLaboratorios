<%= form_with(model: hora) do |form| %>
  <% if hora.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hora.errors.count, "error") %> prohibited this hora from being saved:</h2>

      <ul>
        <% hora.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :intervalo, style: "display: block" %>
    <%= form.text_field :intervalo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
